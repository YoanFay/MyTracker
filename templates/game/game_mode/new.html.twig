{% extends 'base.html.twig' %}

{% block title %}New GameMode{% endblock %}

{% block body %}

    <div class="card">
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>

    <a href="{{ path('game_mode') }}">back to list</a>
{% endblock %}
