{% extends 'base.html.twig' %}

{% block title %}GameMode{% endblock %}

{% block body %}
    <h1>GameMode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_mode.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game_mode.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_mode_index') }}">back to list</a>

    <a href="{{ path('game_mode_edit', {'id': game_mode.id}) }}">edit</a>

    {{ include('game/game_mode/_delete_form.html.twig') }}
{% endblock %}
