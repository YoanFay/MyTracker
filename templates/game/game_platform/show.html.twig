{% extends 'base.html.twig' %}

{% block title %}GamePlatform{% endblock %}

{% block body %}
    <h1>GamePlatform</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_platform.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game_platform.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_platform_index') }}">back to list</a>

    <a href="{{ path('game_platform_edit', {'id': game_platform.id}) }}">edit</a>

    {{ include('game/game_platform/_delete_form.html.twig') }}
{% endblock %}
