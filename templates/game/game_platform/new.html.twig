{% extends 'base.html.twig' %}

{% block title %}New GamePlatform{% endblock %}

{% block body %}

    <div class="card">
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>

    <a href="{{ path('game_platform_index') }}">back to list</a>
{% endblock %}
