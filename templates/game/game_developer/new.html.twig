{% extends 'base.html.twig' %}

{% block title %}New GameDeveloper{% endblock %}

{% block body %}

    <div class="card">
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>

    <a href="{{ path('game_develope') }}">back to list</a>
{% endblock %}
