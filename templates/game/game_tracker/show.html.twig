{% extends 'base.html.twig' %}

{% block title %}GameTheme{% endblock %}

{% block body %}
    <h1>GameTheme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_theme.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game_theme.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_theme_index') }}">back to list</a>

    <a href="{{ path('game_theme_edit', {'id': game_theme.id}) }}">edit</a>

    {{ include('game/game_theme/_delete_form.html.twig') }}
{% endblock %}
