{% extends 'base.html.twig' %}

{% block title %}Edit GameTheme{% endblock %}

{% block body %}

    <div class="card">
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>

    <a href="{{ path('game_theme_index') }}">back to list</a>
{% endblock %}
