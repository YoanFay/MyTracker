{% extends 'base.html.twig' %}

{% block title %}GamePublishers{% endblock %}

{% block body %}
    <h1>GamePublishers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_publisher.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game_publisher.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_publishers') }}">back to list</a>

    <a href="{{ path('game_publishers_edit', {'id': game_publisher.id}) }}">edit</a>

    {{ include('game/game_publishers/_delete_form.html.twig') }}
{% endblock %}
