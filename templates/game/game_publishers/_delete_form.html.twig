<form method="post" action="{{ path('game_publishers_delete', {'id': game_publisher.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game_publisher.id) }}">
    <button class="btn">Delete</button>
</form>
