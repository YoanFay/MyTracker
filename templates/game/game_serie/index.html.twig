{% extends 'base.html.twig' %}

{% block title %}GameSerie index{% endblock %}

{% block body %}
    <h1>GameSerie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for game_serie in game_series %}
            <tr>
                <td>{{ game_serie.id }}</td>
                <td>{{ game_serie.name }}</td>
                <td>
                    <a href="{{ path('game_serie_show', {'id': game_serie.id}) }}">show</a>
                    <a href="{{ path('game_serie_edit', {'id': game_serie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('game_serie_new') }}">Create new</a>
{% endblock %}
