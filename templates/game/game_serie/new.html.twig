{% extends 'base.html.twig' %}

{% block title %}New GameSerie{% endblock %}

{% block body %}

    <div class="card">
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>

    <a href="{{ path('game_serie') }}">back to list</a>
{% endblock %}
