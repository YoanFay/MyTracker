{% extends 'base.html.twig' %}

{% block title %}GameSerie{% endblock %}

{% block body %}
    <h1>GameSerie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_serie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game_serie.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_serie_index') }}">back to list</a>

    <a href="{{ path('game_serie_edit', {'id': game_serie.id}) }}">edit</a>

    {{ include('game/game_serie/_delete_form.html.twig') }}
{% endblock %}
