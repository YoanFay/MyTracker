{% extends 'base.html.twig' %}

{% block title %}New GameGenre{% endblock %}

{% block body %}

    <div class="card">
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>

    <a href="{{ path('game_genre') }}">back to list</a>
{% endblock %}
