{% extends 'base.html.twig' %}

{% block title %}GameGenre{% endblock %}

{% block body %}
    <h1>GameGenre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_genre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game_genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_genre_index') }}">back to list</a>

    <a href="{{ path('game_genre_edit', {'id': game_genre.id}) }}">edit</a>

    {{ include('game/game_genre/_delete_form.html.twig') }}
{% endblock %}
